{% extends 'partials/_master.twig' %}

{% block main %}

    {% if record is defined %}

        <h1>{{ record.title }}</h1>

        {{ record.teaser }}

        {{ record.body }}

    {% endif %}

{% endblock main %}
